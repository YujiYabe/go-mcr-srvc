openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0

servers:
  - url: https://api.example.com/v1

paths:
  /v1/users:
    $ref: './paths/v1/users.yaml'
  /v1/health:
    $ref: './paths/v1/health.yaml'
  /v1/to-pubsub:
    $ref: './paths/v1/to-pubsub.yaml'
  /v1/validation-word-rules:
    $ref: './paths/v1/validation-word-rules.yaml'

components:
  schemas:
    User:
      $ref: './components/schemas/User.yaml'
    Error:
      $ref: './components/schemas/Error.yaml'
    ValidationWordRule:
      $ref: './components/schemas/ValidationWordRule.yaml'
    ValidationWordRuleCreate:
      $ref: './components/schemas/ValidationWordRuleCreate.yaml'
    ValidationWordRuleUpdate:
      $ref: './components/schemas/ValidationWordRuleUpdate.yaml'
    ValidationWordRuleDelete:
      $ref: './components/schemas/ValidationWordRuleDelete.yaml'

  responses:
    NotFound:
      $ref: './components/responses/NotFound.yaml'
