get:
  summary: Check service health
  operationId: v1HealthGet
  responses:
    '200':
      description: Service is healthy
      content:
        text/plain:
          schema:
            type: string
            example: "OK"
