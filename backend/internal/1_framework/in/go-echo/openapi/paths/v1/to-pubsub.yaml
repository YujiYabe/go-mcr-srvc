get:
  summary: send to pubsub
  operationId: v1ToPubsubGet
  responses:
    '200':
      description: test
      content:
        text/plain:
          schema:
            type: string
            example: "OK"
